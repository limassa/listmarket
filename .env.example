# Supabase: Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# URL pública do site (obrigatório em Railway/Vercel: evita redirect OAuth para localhost:PORT)
NEXT_PUBLIC_SITE_URL=https://listmarket-production.up.railway.app

# No painel Supabase: Authentication → URL configuration
# Site URL: http://localhost:3000 (ou sua URL de produção)
# Redirect URLs: inclua /auth/callback e /auth/redefinir-senha (redefinição de senha)
# Ex.: http://localhost:3000/auth/callback
#      http://localhost:3000/auth/redefinir-senha
#
# Ative o provedor Google em Authentication → Providers → Google
#
# E-mails (link mágico / convite): personalize em Authentication → Email Templates.
# Imagens no HTML do template: use URLs públicas https (ex.: o deploy + /lista-mercado-app-logo.svg).
#
# SQL: cole e execute supabase/schema.sql no SQL Editor
#
# APK (Capacitor): URL que o WebView abre — deploy (Vercel) ou PC na rede:
# CAPACITOR_SERVER_URL=https://seu-app.vercel.app
# ou, no mesmo Wi‑Fi: CAPACITOR_SERVER_URL=http://192.168.x.x:3000
# Depois: npm run cap:sync && npm run android:apk (precisa Android Studio / JDK)
